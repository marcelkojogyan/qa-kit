# =============================================================================
# QA Kit Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Base URL for the application under test
APP_BASE_URL=http://localhost:3000

# Backend API URL (if different from APP_BASE_URL)
API_BASE_URL=http://localhost:3001/api

# -----------------------------------------------------------------------------
# E2E Test Endpoints (Backend)
# -----------------------------------------------------------------------------
# Secret key for authenticating with test endpoints
# Generate with: openssl rand -hex 32
E2E_BOOTSTRAP_SECRET=your-secret-here

# -----------------------------------------------------------------------------
# Supabase Configuration (if using Supabase)
# -----------------------------------------------------------------------------
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# Test Persona Credentials
# -----------------------------------------------------------------------------
# Peter - Power User Persona
E2E_PETER_EMAIL=peter.power+e2e@piro.test
E2E_PETER_PASSWORD=

# Mary - Basic User Persona (example)
# E2E_MARY_EMAIL=mary.basic+e2e@piro.test
# E2E_MARY_PASSWORD=

# -----------------------------------------------------------------------------
# Visual Regression Tracker (VRT)
# -----------------------------------------------------------------------------
# VRT service URLs (local Docker setup)
VRT_API_URL=http://localhost:4200
VRT_UI_URL=http://localhost:8080

# VRT project and authentication
VRT_PROJECT=piro
VRT_API_KEY=

# VRT branch - defaults to current git branch
# VRT_BRANCH=main

# VRT CI build ID - set automatically in CI
# VRT_CI_BUILD_ID=

# -----------------------------------------------------------------------------
# Playwright Settings
# -----------------------------------------------------------------------------
# Run tests in headed mode (useful for debugging)
# HEADED=true

# Enable video recording (on-failure, always, never)
# VIDEO=on-failure

# Enable trace recording (on-failure, always, never)
# TRACE=on-failure

# Timeout for tests in milliseconds
# TEST_TIMEOUT=60000

# -----------------------------------------------------------------------------
# CI/CD Settings
# -----------------------------------------------------------------------------
# Set to true in CI environment
# CI=true

# Number of parallel workers in CI
# CI_WORKERS=4
