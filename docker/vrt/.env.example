# =============================================================================
# Visual Regression Tracker (VRT) Docker Configuration
# =============================================================================
# Copy this file to .env in this directory before running docker-compose

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
VRT_DB_USER=vrt
VRT_DB_PASSWORD=vrtpassword
VRT_DB_NAME=vrt

# -----------------------------------------------------------------------------
# S3/MinIO Storage Configuration
# -----------------------------------------------------------------------------
VRT_S3_ACCESS_KEY=minioaccess
VRT_S3_SECRET_KEY=miniosecret
VRT_S3_BUCKET=vrt-images
VRT_S3_REGION=us-east-1

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# Generate a secure secret: openssl rand -hex 32
VRT_JWT_SECRET=your-super-secret-jwt-key-change-in-production
VRT_JWT_LIFETIME=1d

# -----------------------------------------------------------------------------
# Port Configuration
# -----------------------------------------------------------------------------
# VRT API (Playwright agent connects here)
VRT_API_PORT=4200

# VRT UI (Dashboard for reviewing diffs)
VRT_UI_PORT=8080

# MinIO ports (S3-compatible storage)
VRT_MINIO_PORT=9000
VRT_MINIO_CONSOLE_PORT=9001
